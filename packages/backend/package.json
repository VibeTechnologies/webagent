{
  "name": "@vibetechnologies/webagent-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply webagent-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply webagent-db",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "vitest": "^3.1.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.10.0"
  }
}
